export type InstitutionDto = {
    bic: string;
    countries: string[];
    id: string;
    logo: string;
    max_access_valid_for_days: string;
    name: string;
    transaction_total_days: string;
}